public class JavaPdfHelloWorld
{
	/*
	 * public static void main(String[] args) { Document document = new Document();
	 * try { PdfWriter writer = PdfWriter.getInstance(document, new
	 * FileOutputStream("D:/documents/Kigali/HelloWorld.pdf")); document.open(); //
	 * Font font = FontFactory.getFont(FontFactory.COURIER, 16, BaseColor.BLACK);
	 * //Chunk chunk = new Chunk("Hello World", font);
	 * 
	 * //document.add(chunk); document.addHeader("BOK","Privacy Statement");
	 * 
	 * System.out.println("#########"); document.close(); writer.close(); } catch
	 * (DocumentException e) { e.printStackTrace(); } catch (FileNotFoundException
	 * e) { e.printStackTrace(); } }
	 */}